#include<iostream>
using namespace std;
 
int fibonacci(int n)
{
    if((n==1)||(n==0))
    {
        return(n);
    }
    else
    {
        return(fibonacci(n-1)+fibonacci(n-2));
    }
}
 
int main()
{  int t;
cin>>t;
while(t--)
{
    int n,i=0;
    cin>>n;
    int arr[n];
    while(i<n)
    {
     arr[i]=(fibonacci(i)%10);
        i++;
    }
   while(n!=2)
   {
   for(int i=0;i<n;i++)
   {
       while(n!=2)
       {
           arr[i]=arr[i+1]
       }
   }
   }
}
    return 0;
}
